# AWS-BackUp-Audit-Script
Runs a point-in-time security and backup audit against your AWS account. For each control it reports PASS, FAIL, WARN, or SKIP along with a plain-English detail message and a one-line remediation command.  Results are displayed in the terminal and exported to Excel (one tab per service) or JSON.
